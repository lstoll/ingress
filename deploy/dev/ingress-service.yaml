apiVersion: v1
kind: Service
metadata:
  name: ingress
spec:
  selector:
    app: ingress
  type: NodePort
  ports:
    - name: tls
      port: 443
      targetPort: 443
      nodePort: 30443
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
